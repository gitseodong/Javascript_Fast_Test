// 나머지 매개변수 (rest parameter)
// 전개연산자 사용

function sum(...rest) {
  return rest.reduce((a, b) => a + b, 0);
}

console.log(sum(1, 2, 3, 4, 5, 6, 7)); // 28
console.log(sum(10, 11, 12, 13, 14, 15)); // 75
