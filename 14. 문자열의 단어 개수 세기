let fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().trim().split(' ');
if (input[0] === '') {
    console.log(0);
} else {
   console.log(input.length); 
}

// 단어가 없는 경우에도 빈문자열이 1개 존재하여 1로 출력되므로 0으로 출력되도록 if문을 작성한다.
